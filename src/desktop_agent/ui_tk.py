"""Legacy Tkinter UI (kept for reference).

The project switched to a PySide6 UI in `desktop_agent.ui`.
This module is not imported by default.
"""

from __future__ import annotations

# NOTE: This file intentionally left as a minimal stub so older imports/tests
# don't break if someone references it. The full previous implementation lived
# in `desktop_agent.ui`.

