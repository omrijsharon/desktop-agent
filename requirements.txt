# Runtime dependencies
mss
pillow
openai>=1.0.0
# OpenAI Agents SDK (for MCP integration, e.g. Playwright)
openai-agents>=0.8.1
PySide6>=6.6
tensorboard>=2.12
pywinpty>=3.0.3

# Add runtime dependencies here as they are introduced.
# Example (later):
# pyside6
